Q- what is SPA Navigation

- Using React Router.
  No page refresh.

Q- passing children in {MainLayout} - using props

Q- Agar component ko data parent se milna hai → type Props banao aur { props } use karo. (Reusable Component)

Q- What is Reconciliation.

- When state/data changes react create a virtual dom and compare old vs new, updates only changed parts - this process is called reconciliation.

Q- Using React Query - handles = (caching, loading, errors, refetching)
react query is tanstack query - it's powerfull for data fetching and state management - (it's library)

when we try to fetch data from an API - we do - useState, useEffect, loading state, error on our own. But tanStack query handle all of this on own. 

Also - Caching - if data fetch then save it and don't fetch next time.
when window is focused then data fetched automatically.
give - isLoading and isError in default.


